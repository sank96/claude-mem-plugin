{
  "name": "claude-mem-plugin",
  "version": "0.1.0",
  "private": true,
  "description": "Cross-CLI integration layer for claude-mem.",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test tests/package/package-metadata.test.js tests/docs/task1-contract.test.js"
  }
}
