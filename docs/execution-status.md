# Execution Status

This file is a thin human-readable pointer. The live state lives in `docs/execution-status.json` and the dashboard renders that JSON directly.

## What to use

- `docs/execution-status.json` for live structured state
- `docs/dashboard.html` for the visual summary

## Purpose

Keep this file short so it does not drift from the canonical JSON state.
